{
  "manifest_version": 3,
  "name": "Paprika Agent",
  "version": "0.4.0",
  "description": "Built-in Paprika worker helper. Exposes a generic command bus for Chrome capabilities CDP / nodriver can't reach directly (genuine page zoom, request header/block rules, content settings, privacy, downloads, proxy) plus an operator-event recorder for programming-by-demonstration learning. Driven by the worker via a page<->content-script<->service-worker relay; has no UI.",
  "permissions": [
    "tabs",
    "storage",
    "declarativeNetRequest",
    "declarativeNetRequestWithHostAccess",
    "contentSettings",
    "privacy",
    "webNavigation",
    "userScripts",
    "management",
    "downloads",
    "proxy"
  ],
  "host_permissions": [
    "<all_urls>"
  ],
  "background": {
    "service_worker": "background.js"
  },
  "content_scripts": [
    {
      "matches": ["<all_urls>"],
      "all_frames": false,
      "run_at": "document_start",
      "js": ["content.js"]
    }
  ]
}
